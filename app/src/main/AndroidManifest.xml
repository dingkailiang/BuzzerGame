<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.dingkaiualberta.buzzergame" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".UI.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.SingleGameActivity"
            android:label="@string/title_activity_single_game" >
        </activity>
        <activity
            android:name=".UI.MultiGameActivity"
            android:label="@string/title_activity_multi_game" >
        </activity>
        <activity
            android:name=".UI.StatsActivity"
            android:label="@string/title_activity_stats" >
        </activity>
    </application>

</manifest>
